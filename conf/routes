POST        /github-webhook         controllers.JenkinsProxy.proxiedUnix
POST        /github-webhook/        controllers.JenkinsProxy.proxiedUnix
POST        /github-windows         controllers.JenkinsProxy.proxiedWindows
POST        /github-windows/        controllers.JenkinsProxy.proxiedWindows
